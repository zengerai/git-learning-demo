# Git 实战学习笔记

## 📅 第一阶段：基础三板斧 (保存存档)
**核心概念**：工作区 -> 暂存区 -> 仓库

- **`git status`**：查看当前状态（哪里变了？）。
- **`git add <file>`**：把文件放入暂存区（准备提交）。
- **`git commit -m "message"`**：把暂存区的内容永久保存到仓库（存档）。
- **`git log`**：查看历史提交记录（查看存档）。

## 🎭 第二阶段：分支魔法 (平行宇宙)
**核心概念**：分支 (Branch) 是独立的平行宇宙，互不干扰。

- **`git branch <name>`**：创建一个新分支。
- **`git branch`**：查看当前有哪些分支。
- **`git switch <name>`** (或 `git checkout`)：切换到指定分支。
- **`git merge <name>`**：把指定分支的内容合并到当前分支。
- **`git branch -d <name>`**：删除分支（合并后通常会删除）。

## 🥊 第三阶段：冲突解决 (做裁判)
**核心概念**：当两个人修改了同一行代码，Git 无法自动合并，需要人工介入。

1. **发现冲突**：`git merge` 时提示 `CONFLICT`。
2. **查看代码**：文件中会出现冲突标记：
   ```python
   <<<<<<< HEAD
   print('当前分支的内容')
   =======
   print('合并进来的分支的内容')
   >>>>>>> branch-name
   ```
3. **手动修复**：删除标记符号，保留你想要的代码。
4. **提交结果**：
   - `git add <file>`
   - `git commit -m "Fix conflict"`
